extends Node

# This script just makes mouse invisble unless ESC is pressed
func _ready() -> void:
	
	# Input.MOUSE_MODE_CAPTURED:
	# Captures the mouse. 
	# The mouse will be hidden and its position locked at the center of the screen.
	#Input.set_mouse_mode(Input.MOUSE_MODE_CAPTURED)
	pass
	
#func _input(event) -> void:
#	#if event.is_action_pressed("ui_cancel"):
#	#	Input.set_mouse_mode(Input.MOUSE_MODE_VISIBLE)
#	pass
